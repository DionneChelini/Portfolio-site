import { Container } from '@/components/Container'
import { Section } from '@/components/Section'
const loading = () => {
  return (
    <Container>
      <div className="tect-2xl flex h-screen items-center justify-center font-semibold">
        Loading...
      </div>
    </Container>
  )
}
export default loading
